# Example environment variables for client
# Copy to .env or set in your CI secrets

# Public API URL for server (used by client axios config).
# Leave blank when frontend and backend are served behind the same domain
# because the app defaults to /api.
VITE_API_URL=

# Mapbox or MapLibre token if used
VITE_MAPBOX_TOKEN=
